---
# vars file for aws-sg-whitelist-my-ip

security_groups:
  - name: launch-wizard-1
    rules:
    - proto: tcp
      from_port: 80
      to_port: 80
      cidr_ip: "{{my_ip}}"
    - proto: tcp
      from_port: 443
      to_port: 443
      cidr_ip: "{{my_ip}}"